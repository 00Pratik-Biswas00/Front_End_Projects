{
  /* phone number */
}
{
  /* <div className="flex gap-x-3 items-center justify-center w-full p-5">
          <select
            className="text-black border p-1 px-3  cursor-pointer"
            onChange={handleSelect}
          >
            {countryName.map((option, i) => (
              <option value={option.value}>{option.label}</option>
            ))}
          </select>
          <p className="text-white">{value}</p>
          <input
            className="bg-transparent border-b py-3 outline-none w-full transition-all text-white"
            type="text"
            name="phone_no"
            placeholder="Phone Number"
          />
        </div> */
}

{
  /* passwords */
}
{
  /* <div className="flex items-center justify-center w-full">
          <input
            className="bg-transparent border-b py-3 outline-none w-full transition-all text-white"
            type={visible ? "text" : "password"}
            name="password"
            // required
            placeholder="Password"
          />

          <div
            className="text-white border p-1 rounded-md cursor-pointer"
            onClick={() => setVisible(!visible)}
          >
            {visible ? "Hide" : "Unhide"}
          </div>
        </div> */
}

// //password
// const [visible, setVisible] = useState(false);

// // phone number
// const [value, setValue] = useState("");
// const countryName = [
//   { label: "Country", value: 0 },
//   { label: "India", value: "+91" },
//   { label: "Argentina", value: "+54" },
//   { label: "Brazil", value: "+55" },
// ];
